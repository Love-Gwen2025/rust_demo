// ============================================
// Demo 02: 数据类型
// 运行: cargo run --example 02_data_types
// ============================================

fn main() {
    // ---------- 整数类型 ----------
    let a: i8 = -128; // 有符号 8 位
    let b: u8 = 255; // 无符号 8 位
    let c: i32 = 2_147_483_647; // 有符号 32 位（默认）
    let d: i64 = 9_000_000_000_000_000_000;
    println!("整数: a={}, b={}, c={}, d={}", a, b, c, d);

    // ---------- 浮点数 ----------
    let f1 = 3.14; // 默认 f64
    let f2: f32 = 2.71828; // 显式 f32
    println!("浮点数: f1={}, f2={}", f1, f2);

    // ---------- 布尔类型 ----------
    let is_rust_fun: bool = true;
    println!("Rust 有趣吗? {}", is_rust_fun);

    // ---------- 字符类型 ----------
    let ch: char = '中'; // 4 字节，支持 Unicode
    let emoji = '🦀';
    println!("字符: {}, {}", ch, emoji);

    // ---------- 元组 (Tuple) ----------
    let tup: (i32, f64, char) = (500, 6.4, '✨');
    let (x, y, z) = tup; // 解构
    println!("元组解构: x={}, y={}, z={}", x, y, z);
    println!("元组索引: tup.0={}, tup.1={}", tup.0, tup.1);

    // ---------- 数组 (Array) ----------
    let arr: [i32; 5] = [1, 2, 3, 4, 5]; // 固定长度
    let arr2 = [3; 5]; // [3, 3, 3, 3, 3]
    println!("数组: arr[0]={}, arr2={:?}", arr[0], arr2);
}
